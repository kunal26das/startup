package io.github.kunal26das.startup.sample

/** The BrowserCrashReporter build of the sample. */
actual object Platform {

    /** Names the SDK a real integration would start here. */
    actual val crashReportingSdk: String = "BrowserCrashReporter"
}
