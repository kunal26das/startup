package io.github.kunal26das.startup.sample

/** Which of the two runtimes planned and created this application's components. */
class RuntimeInfo(
    /** The library that ordered the graph, as a Maven-style name. */
    val name: String,
)
